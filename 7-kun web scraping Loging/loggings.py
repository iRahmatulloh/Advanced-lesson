import logginglogger = logging.getLogger(__name__)logger.setLevel(logging.DEBUG)format = logging.Formatter("%(asctime)s : %(filename)s : %(levelname)s : %(message)s\n")file = logging.FileHandler('meta.log')file.setFormatter(format)logger.addHandler(file)def add(x, y):    return x + ydef sub(x, y):    return x - yx, y = 23, 65logger.debug(add(x,y))logger.debug(sub(x,y))# import bs4# import requests# web = requests.get('https://dsa-uz.vercel.app/bigo')# print(web.text.find('<a'))# html = web.text# soup = bs4.BeautifulSoup(html, 'html.parser')# print(soup.find('iframe')['src'])